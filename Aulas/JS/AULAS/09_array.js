let nomes = ["Gustavo", "Henrique","Mauricio"]//adcionar mas de uma coisa na variavel
let i1 = 0;
console.log(nomes);
console.log(nomes[0]);//escolher posiçado da lista

nomes[3] = "Kasinão" //substituiir nomes da lista
console.log(nomes);
console.log(nomes.length);

while (i1 <nomes.length) {//contador
    console.log(nomes[i1]); // i para mostrar o número que o nome se localiza
    i++ //soma mais 1
}

let numeros = [10,17,13,22,5]
let i = 0;
let soma = 0;
console.log(numeros.length);
console.log(numero[2]);

while (i<numero.length) {
    soma += numero [i];
    i++;
}